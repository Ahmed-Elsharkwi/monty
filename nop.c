#include"monty.h"
/**
 * nop - it doesn't do anything
 * @stack: is a poitner for the stack
 * @line_number: is a line number
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) **stack;
}
